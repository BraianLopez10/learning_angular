import { RepeatProdPipe } from './repeat-prod.pipe';

describe('RepeatProdPipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatProdPipe();
    expect(pipe).toBeTruthy();
  });
});
